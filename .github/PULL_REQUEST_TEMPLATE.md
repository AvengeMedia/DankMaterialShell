## Description

<!-- What does this PR do? Why is it needed? -->

## Motivation

<!-- Link to issue if applicable. Explain the problem this solves. -->

## Changes

<!-- List key changes. One logical change per commit preferred. -->

-
-

## Screenshots / Video

<!-- Required if visual changes are present. Before/after for UI changes. -->

## Pre-PR Checklist

```bash
# Format QML
cd quickshell && ./qmlformat-all.sh

# Lint QML (requires .qmlls.ini generated by qs -p .)
make lint-qml

# Go (only if core/ was changed)
cd core && make fmt && make test && go mod tidy
```

## PR Checklist

- [ ] Tested on at least one compositor (Niri, Hyprland, etc.)
- [ ] QML formatted (`qmlformat-all.sh`)
- [ ] No regressions in default/normal mode
- [ ] New settings default to current behavior
- [ ] User-facing strings wrapped in `I18n.tr()` with context
- [ ] Screenshots/video included if visual changes
- [ ] Commit messages follow conventional format (`feat:`, `perf:`, `fix:`, etc.)

## Code Style Reminders

- 4-space indentation in QML
- No comments unless essential for complex logic
- Use `DankIcon` for all icons, not manual Text components
- Bind directly to service properties, avoid wrapper functions
- Use `Theme.propertyName` for all styling values
